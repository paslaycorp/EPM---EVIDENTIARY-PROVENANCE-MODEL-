# Circular Discriminator Counterexample

Suppose D is selected because X is expected, while X is then treated as resolved because D was selected. The dependency graph is circular.

**Lesson:** selecting a discriminator cannot be justified by the outcome it is intended to establish.
