# Expected Answer Leakage Counterexample

An analyst predicts X, then chooses only measurements that can distinguish X from Y in a way favorable to X, and finally treats the resulting constraint as neutral.

**Lesson:** expectations can contaminate both constraint selection and discriminator selection. The provenance record must preserve who selected what and why.
