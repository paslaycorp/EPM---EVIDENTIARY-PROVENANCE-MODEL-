# Singleton Answer-Space Counterexample

A constraint reduces `{X,Y}` to `{X}`.

This looks like resolution, but the model must inspect whether the exclusion of Y was validly established. If the exclusion is merely a derived assumption, `{X}` is not equivalent to evidence that X is true.

**Lesson:** cardinality one does not by itself prove epistemic resolution.
