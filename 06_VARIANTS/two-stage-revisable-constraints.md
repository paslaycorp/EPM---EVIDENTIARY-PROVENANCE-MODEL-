# Revisable-Constraints Variant

**Status:** PROVISIONAL

Constraints are versioned objects rather than permanent exclusions. New evidence may invalidate a premise and reopen previously excluded candidates. The provenance graph therefore records revisions instead of mutating history invisibly.
