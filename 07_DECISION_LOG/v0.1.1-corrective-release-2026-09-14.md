# EPM v0.1.1 Corrective Release Decision — 2026-09-14

## Decision

Release EPM `0.1.1` as a narrow corrective patch over the frozen `0.1.0` standalone runtime.

## Trigger

The isolated TVC falsification branch exposed a reproducible temporal-admissibility defect in EPM v0.1.0: aggregate state inspection accepted a governing rule whose `effective_at` occurred after the epistemic state's historical time.

The defect qualified for reopening under the freeze rule because it was demonstrated by executable evidence rather than architectural speculation.

## Correction

PR #5 added explicit rule/state temporal comparison during `inspect_state()` and regression coverage for future-effective, boundary-equal, uncomparable, and missing state times.

PR #5 merged to `main` at:

`639a719ac7344605ee536157312130b5888dc189`

Post-merge EPM Runtime CI #34 — run `34808791118` — SUCCESS.

## Release candidate

The initial v0.1.1 release candidate head:

`725d9d776e00b906e863261af15d08208c8fea8c`

passed EPM Runtime CI #41 — run `34809214688` — on Python 3.12 and 3.13, including lint, compilation, public import, certification suite, and distribution build.

The release branch also preserves this evidence in `04_TESTS/EPM-CONFORMANCE-MATRIX-v0.1.1.md`; a final CI run after evidence-document insertion is required before merge.

## Boundary

The release does not merge TVC. TVC remains an isolated falsification experiment and has itself been blocked from promotion by executable topology-history, graph-completeness, and dependency-identity attacks.

The v0.1.1 patch does not change answer-space semantics, the epistemic ladder, transition authorization, or connector claims.

## Production integration rule

FAP-Insurance currently pins EPM v0.1.0 commit `b2717613dbbb6575f58b8e334a0fca96cf471bab`.

After v0.1.1 merges, FAP must be repinned to the exact v0.1.1 merge commit and pass its full production verification/CI path before the production integration is considered upgraded.
